{
  "name": "expenses",
  "scripts": {
    "dev": "bun run --hot server/src/index.ts",
    "build": "bun run server/src/index.ts"
  },
  "dependencies": {
    "hono": "^4.5.1",
    "standard-version": "^9.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
